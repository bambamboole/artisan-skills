#!/usr/bin/env python3
import json


CONTEXT = """ARTISAN: apply a skill only when its trigger fits.
Plan or scope non-trivial work → shape.
Implement change → build.
Debug failure → debug.
Review diff or tests → review.
Split independent tasks → parallel.
Build or audit UI → frontend-design.
Explore UI directions → visual-brainstorming.
Build or review PHP → php.
Build or refine React → react.
Build or review TypeScript → typescript.
Write or edit technical prose → writing.
Commit, branch, push, PR → git-and-github.
Inspect first; reuse before adding; keep diffs minimal; verify changed behavior. Details live in skill bodies; no process on simple questions."""

print(json.dumps({"hookSpecificOutput": {"hookEventName": "SessionStart", "additionalContext": CONTEXT}}))

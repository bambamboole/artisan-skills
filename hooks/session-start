#!/usr/bin/env python3
import json


CONTEXT = """ARTISAN: apply a skill only when its trigger fits.
Plan or scope non-trivial work → artisan-shape.
Implement change → artisan-build.
Debug failure → artisan-debug.
Review diff or tests → artisan-review.
Split independent tasks → artisan-parallel.
Build or audit UI → artisan-frontend-design.
Explore UI directions → artisan-visual-brainstorming.
Build or review PHP → artisan-php.
Build or refine React → artisan-react.
Build or review TypeScript → artisan-typescript.
Write or edit technical prose → writing.
Commit, branch, push, PR → git-and-github.
Inspect first; reuse before adding; keep diffs minimal; verify changed behavior. Details live in skill bodies; no process on simple questions."""

print(json.dumps({"hookSpecificOutput": {"hookEventName": "SessionStart", "additionalContext": CONTEXT}}))

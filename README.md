# Artisan Skills

Compact, trigger-based engineering and frontend skills for Codex and Claude.

Run `bin/install` to link the skills, then run `bin/check` to verify them. The plugin emits a concise skill router at session start.
